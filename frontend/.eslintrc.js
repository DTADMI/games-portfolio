// Legacy ESLint config disabled in favor of the root flat config (eslint.config.mjs).
// Keeping this file minimal avoids conflicts like "Cannot find plugin '@typescript-eslint'"
// or duplicate plugin registrations when running `eslint .` from this workspace.
module.exports = {};
