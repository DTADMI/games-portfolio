name: (Archived) Deploy Backend to Cloud Run

on:
  # Archived workflow: disabled by default. Use unified ci-cd.yml for Cloud Run deploys.
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE: games-backend

jobs:
  deploy-backend:
    name: Archived (use ci-cd.yml)
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "This workflow is archived. Use .github/workflows/ci-cd.yml for Cloud Run deploys."
