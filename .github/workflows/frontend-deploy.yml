name: (Archived) Deploy Frontend to Firebase Hosting

on:
  # Archived workflow: keep disabled by default. Manually run only if ever needed.
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Archived (not in use; frontend runs on Cloud Run)
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "This workflow is archived. Frontend is deployed to Cloud Run via ci-cd.yml."
