name: (Archived) CI (use unified ci-cd.yml)

on:
  # Archived: superseded by .github/workflows/ci-cd.yml
  workflow_dispatch:

jobs:
  archived:
    name: Archived (use ci-cd.yml)
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "This workflow is archived. Use .github/workflows/ci-cd.yml."
