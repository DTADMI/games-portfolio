name: (Archived) Deploy Snake Game

on:
  # Archived: this project deploys the SSR frontend on Cloud Run.
  # Individual game hosting on Firebase is no longer used.
  workflow_dispatch:

env:
  FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
  FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
  NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL || 'https://your-api-url.com/api' }}

jobs:
  deploy-snake:
    name: Archived (not in use)
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "This workflow is archived. Games are served via the unified Next.js frontend on Cloud Run."
